#include <stdlib.h>

#include "stack.h"

// the stack is being implemented as linked lists in order to simplify traverse operations.

int main() {
    Stack stack;
    stack_init(&stack, NULL);
    return 0;
}
